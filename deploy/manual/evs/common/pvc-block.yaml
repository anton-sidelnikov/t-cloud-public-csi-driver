apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evs-block-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 10Gi
  storageClassName: tcloud-public-evs-manual
