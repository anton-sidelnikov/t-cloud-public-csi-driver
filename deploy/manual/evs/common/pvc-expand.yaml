apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evs-expand-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: tcloud-public-evs-manual
